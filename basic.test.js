const expect = require("chai").expect;
const func = require("./app");

//Point to exact folder
//npm run mocha ./
describe("func", () => {
  it("", () => {
    expect(func("")).to.equal("");
  });
});
