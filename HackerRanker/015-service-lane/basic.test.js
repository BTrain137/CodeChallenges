const expect = require("chai").expect;
const serviceLane = require("./app");

//Point to exact folder
//npm run mocha ./HackerRanker/015-serviceLane
describe("serviceLane", () => {
  it("", () => {
    expect(serviceLane()).to.deep.equal();
  });

  it("", () => {
    expect(serviceLane()).to.deep.equal();
  });
});
