const expect = require("chai").expect;
const saveThePrisoner = require("./app");

//Point to exact folder
//npm run mocha ./HackerRanker/016-saveThePrisoner
describe("saveThePrisoner", () => {
  it("", () => {
    expect(saveThePrisoner()).to.deep.equal();
  });

  it("", () => {
    expect(saveThePrisoner()).to.deep.equal();
  });
});
